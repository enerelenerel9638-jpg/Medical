<UserControl x:Class="MedicialUse.Components.Calendar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:MedicialUse.Components"
             mc:Ignorable="d" Width="460">
    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0" Margin="0,0,0,8">
            <StackPanel Orientation="Horizontal" DockPanel.Dock="Left" >
                <Button Content="◀" Width="36" Margin="0,0,6,0" Click="PrevMonth_Click"/>
                <Button Content="▶" Width="36" Click="NextMonth_Click"/>
            </StackPanel>
            <TextBlock x:Name="Caption"
                    HorizontalAlignment="Center"
                    FontWeight="SemiBold"
                    FontSize="18"/>
        </DockPanel>

        <UniformGrid Grid.Row="1" Columns="7" x:Name="weekdayHeader" Margin="0,0,0,8"/>

        <StackPanel Grid.Row="2">
            <StackPanel Orientation="Horizontal" x:Name="dayRow1"/>
            <StackPanel Orientation="Horizontal" x:Name="dayRow2"/>
            <StackPanel Orientation="Horizontal" x:Name="dayRow3"/>
            <StackPanel Orientation="Horizontal" x:Name="dayRow4"/>
            <StackPanel Orientation="Horizontal" x:Name="dayRow5"/>
            <StackPanel Orientation="Horizontal" x:Name="dayRow6"/>
        </StackPanel>
    </Grid>
</UserControl>
